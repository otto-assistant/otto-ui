/** Minimum delay between outbound Discord PATCH/POST edits to avoid rate limiting. */
export const MIN_DISCORD_EDIT_INTERVAL_MS = 1800;

/** Discord `content` field hard limit. */
export const DISCORD_CONTENT_MAX = 2000;
